# StressSpec - Requirements Stress Tester
# Python dependencies for the project
#
# BEGINNER NOTES:
# - This file lists all the external libraries our project needs
# - It's like a shopping list for Python packages
# - pip install -r requirements.txt will install all of these
# - Version numbers (>=7.0.0) ensure we get compatible versions

# Core CLI dependencies
pytest>=7.0.0      # Testing framework - runs our unit tests
argparse            # Command-line argument parsing (built into Python 3.2+)
pathlib             # Modern file path handling (built into Python 3.4+)
typing-extensions   # Additional type hints for older Python versions

# Web UI dependencies
fastapi>=0.104.0    # Modern, fast web framework for building APIs
uvicorn[standard]>=0.24.0  # ASGI server for running FastAPI applications
jinja2>=3.1.0       # Template engine for HTML rendering
python-multipart>=0.0.6  # Required for file uploads in FastAPI
python-jose[cryptography]>=3.3.0  # JWT token handling (for future auth features)
python-dotenv>=1.0.0  # Environment variable management
aiofiles>=23.0.0    # Async file operations for better performance
